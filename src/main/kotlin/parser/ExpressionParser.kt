package parser

import buffer.BufferElement

class ExpressionParser {

    companion object {
        fun parse(expression: String): List<BufferElement> {
            return listOf()
        }
    }
}